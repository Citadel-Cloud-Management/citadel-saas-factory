apiVersion: v1
kind: Service
metadata:
  name: citadel-backend
  labels:
    app: citadel-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: citadel-backend

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: citadel-backend
spec:
  podSelector:
    matchLabels:
      app: citadel-backend
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8000
          protocol: TCP
